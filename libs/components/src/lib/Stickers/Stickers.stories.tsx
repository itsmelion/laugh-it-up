import type { Meta } from '@storybook/react';
import { Stickers } from './Stickers';

const Story: Meta<typeof Stickers> = {
  component: Stickers,
  title: 'Stickers',
};
export default Story;

export const Primary = {
  args: {},
};
