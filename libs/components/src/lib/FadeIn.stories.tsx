import type { Meta } from '@storybook/react';
import { FadeIn } from './FadeIn';

const Story: Meta<typeof FadeIn> = {
  component: FadeIn,
  title: 'FadeIn',
};
export default Story;

export const Primary = {
  args: {},
};
