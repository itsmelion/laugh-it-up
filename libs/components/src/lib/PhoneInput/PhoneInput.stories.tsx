import type { Meta } from '@storybook/react';
import { PhoneInput } from './PhoneInput';

const Story: Meta<typeof PhoneInput> = {
  component: PhoneInput,
  title: 'PhoneInput',
};
export default Story;

export const Primary = {
  args: {},
};
